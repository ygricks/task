info:
  name: GET_all
  type: http
  seq: 7

http:
  method: GET
  url: "{{HOST}}/task"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return task array", function () {
          expect(res.getStatus()).to.equal(200);
          
          const data = res.getBody();
          expect(data).to.be.an("array");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
