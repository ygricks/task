info:
  name: GET_changed
  type: http
  seq: 5

http:
  method: GET
  url: "{{HOST}}/task/{{TASK_ID}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return task nr.1", function () {
          expect(res.getStatus()).to.equal(200);
          
          const body = res.getBody();
          expect(body).to.have.property("id");
          expect(body.name).to.equal("test changed");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
