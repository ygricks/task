info:
  name: GET_created
  type: http
  seq: 3

http:
  method: GET
  url: "{{HOST}}/task/{{TASK_ID}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return task nr.1", function () {
          expect(res.getStatus()).to.equal(200);
          
          const body = res.getBody();
          expect(body).to.have.property("id");
          expect(body.name).to.equal("test");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
