info:
  name: DELETE_created
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{HOST}}/task/{{TASK_ID}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should remove created task", function () {
          expect(res.getStatus()).to.equal(200);
          bru.deleteEnvVar("TASK_ID");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
